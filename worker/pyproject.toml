[project]
name = "wikimind-worker"
version = "0.0.1"
description = "WikiMind ingest worker — W0 skeleton (PDF/image parsing deferred to v0.2)"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py311"
